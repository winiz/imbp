/* class declarations goes here */
/* §  BpTree(int) – this constructor creates an empty tree, the parameter sets the number of keys that can be stored in each node (leaf and interior)
///§  insert(int, string) – inserts the key (int) and value (string) into the tree
///§  remove(int) – searches the tree for the key (int), if found removes the key and associated value from the tree
///§  find(int) – searches the tree for the key (int), if found returns the matching value
///§  printKeys() – prints the keys in each node with nodes of the same level appearing on the same line
///§  printValues() – prints the values in key (not value) order
 /